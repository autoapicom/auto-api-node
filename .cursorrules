# auto-api-node — Cursor Rules

- TypeScript with strict mode, ESM only
- Single Client class, 6 async methods
- Every method takes `source` as the first parameter
- Built-in fetch (Node 18+), zero runtime dependencies
- Returns plain objects, no wrapper types
- snake_case for API parameters
- ApiError for errors, AuthError for 401/403
- English comments only

## Never

- Add runtime dependencies
- Use CommonJS (require/module.exports)
- Create wrapper/DTO types
- Add unnecessary abstractions
